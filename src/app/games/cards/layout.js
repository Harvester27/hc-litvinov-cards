export const metadata = {
  title: 'Lancers Card | HC Litvínov Lancers',
  description: 'Přihlaste se do Lancers Card a vytvořte si vlastní hokejový tým.',
};

export default function CardsLayout({ children }) {
  return children;
}
